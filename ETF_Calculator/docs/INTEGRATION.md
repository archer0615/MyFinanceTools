# Integration

Frontend `SimulationRequest` fields currently use camelCase.

Backend `SimulationRequest` fields currently use snake_case.

Integration implementation must normalize request payload naming before production API use.
